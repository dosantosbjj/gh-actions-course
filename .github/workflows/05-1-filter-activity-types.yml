name: 05 - Event filters/activity types

on:
    pull_request:
        types: [closed]
        branches: [main]

jobs: 
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Echo the event type
              run: echo "Running whenever a PR is opened or synchronized AND base branch is main"